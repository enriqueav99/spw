{
  "name": "tareas",
  "version": "1.0.0",
  "description": "Backend de la aplicación de tareas",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "DEBUG=app:* nodemon index",
    "start": "NODE_ENV=production node index",
    "build": "tareas build --output public"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.16"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.2",
    "mongodb": "^4.5.0"
  }
}
